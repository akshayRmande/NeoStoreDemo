import { FileUplodDirective } from './file-uplod.directive';

describe('FileUplodDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUplodDirective();
    expect(directive).toBeTruthy();
  });
});
