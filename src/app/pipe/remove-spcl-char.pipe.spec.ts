import { RemoveSpclCharPipe } from './remove-spcl-char.pipe';

describe('RemoveSpclCharPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSpclCharPipe();
    expect(pipe).toBeTruthy();
  });
});
